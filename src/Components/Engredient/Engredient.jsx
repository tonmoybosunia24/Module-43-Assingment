const Engredient = ({ingredient}) => {
       return (
              <div>
                     <ul className="ml-5 list-disc">
                            <li>{ingredient}</li>
                     </ul>
              </div>
       );
};

export default Engredient;